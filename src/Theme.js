import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  primary: "#FFCE68",
  secondary: "#FA8732",
  palette: {
    primary: {
      main: "#616B88",
    },
    secondary: {
      main: "#616B88",
    },
  },
});
